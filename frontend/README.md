# Frontend — CryptoCF

Minimal Vite + React frontend that connects to MetaMask and interacts with the Crowdfund contract.

Quick start

```bash
cd frontend
npm install
npm run dev
```

Before using the UI, deploy `Crowdfund` and update `src/contractConfig.js` `CONTRACT_ADDRESS` with the deployed address.
